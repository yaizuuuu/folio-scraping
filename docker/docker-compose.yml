version: "3.7"
services:
  api:
    build:
      context: ./api
    ports:
      - 80:80
    volumes:
      - ../app/api:/opt/api:cached
  front:
    build:
      context: ./front
    ports:
      - 3000:3000
    volumes:
      - ../app/front:/opt/front:cached